import React, { PureComponent } from "react";
import PropTypes from "prop-types";

class NavbarDivider extends PureComponent {
  render() {
    return <hr className="navbar-divider" />;
  }
}

NavbarDivider.PropTypes = {};

export default NavbarDivider;
